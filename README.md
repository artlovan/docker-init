docker-init
----------

Simple shell script to setup docker on linux machine (tested on empty ec2 on aws)/

Usage (assuming docker-init.sh in already on your machine):

$ sudo su
$ ./docker-init


In word:
imbeding "chown ubuntu /var/run/docker.sock"
